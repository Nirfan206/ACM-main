{
  "name": "al-chaan-meera-server",
  "version": "1.0.0",
  "description": "Backend for AL CHAAN MEERA appliance service app",
  "main": "index.js",
  
"scripts": {
  "start": "node index.js",
  "build": "cd ../client && npm install && chmod +x node_modules/.bin/react-scripts && npm run build"
},
  "dependencies": {
    "bcrypt": "^5.1.0",
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.6.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
